================================================================================
2026-08-11 IMPLEMENTATION PLAN — POS/HO Improvements
================================================================================

Status legend: [ ] = not started   [~] = in progress   [x] = done   [!] = blocked

================================================================================
PHASE 0c — Session hardening (phantom logged-in state fix)
================================================================================

[x] 0c.1 authStore.init: only trust a restored session when supabase.auth
       getUser() validates the access token; if validation fails but a
       stored session exists -> signOut() + clear state (redirect to login)
       instead of rendering the app with 401s on every call
[x] 0c.2 apiFetch (src/lib/api.ts): on 401 from a non-public endpoint when a
       Bearer token was attached -> global signOut + redirect to "/"
       Public paths (pos/login, pos/pin-login, pos/admin-login,
       app/tenant-info, public/tenants, loyalty-cards/create, GET stores)
       are exempt so wrong-PIN 401s behave normally

================================================================================
PHASE 0d — Tenant PIN policy (unique per tenant, admin + staff)
================================================================================

[x] 0d.1 Uniqueness enforced server-side on every PIN write (salted hashes
       make DB constraints impossible; verifyPin scan of tenant users):
       - api/users/create.ts (Vercel): reject if any tenant user matches
       - api/users/[id].ts (Vercel): reject on PIN change, excluding self
       - apiPlugin.ts users/create + put/users/:id (dev parity)
       - api/[...path].ts users/create + users/:id (parity copy)
       -> 400 "This PIN is already in use by another user in this tenant"
[x] 0d.2 PIN length stays 4-8 digits everywhere (no change; keeps existing
       PINs working). Super-admin (super_users) PINs unchanged.

================================================================================
PHASE 0 — Production API outage fix (Vercel serverless)
================================================================================

[x] 0.1 Diagnosis (2026-08-11, evidence from Vercel runtime logs)
       - Symptom: app loads (SPA 200) but ALL /api/* -> 500
         FUNCTION_INVOCATION_FAILED; logbook blank, suppliers/PLU fail
       - Runtime error: ERR_MODULE_NOT_FOUND: Cannot find module
         '/var/task/src/server/apiAuth[.ts|]' imported from
         /var/task/api/[...path].js
       - Vercel compiles each function to .js but does NOT rewrite local
         relative import specifiers and does not bundle files outside api/
         (verified from .vercel/output local build layout: only the
         function .js + node_modules, no src/server)
       - BOTH import styles failed in prod: '.ts' (b0d12b9) and
         extensionless (2h-old deploy) -> /api never worked in production;
         user had only exercised the vite-plugin (dev) API path
       - Canonical fix: explicit '.js' specifiers (file is emitted as
         apiAuth.js) + includeFiles fallback in vercel.json if needed

[x] 0.2 Fix: api/[...path].ts, api/users/[id].ts, api/users/create.ts
       - Change '../src/server/apiAuth' / '../../src/server/apiAuth' ->
         '../src/server/apiAuth.js' / '../../src/server/apiAuth.js'
       - apiPlugin.ts (vite-only) intentionally untouched
[x] 0.2b Route-parity gap: POST /api/pos/pin-login (PIN-first terminal,
       8baa454) existed only in the dev plugin -> 404 on Vercel.
       Ported the full route (admin super-user path + tenant user scan)
       into api/[...path].ts; first-segment route families now match 19/19
[x] 0.3 Local validation: npx tsc -b + npm run build + dev-server smoke
[x] 0.4 Commit + push -> Vercel auto-deploy
[x] 0.5 Verify production (see PHASE 6)

================================================================================
PHASE 1 — Head-office sales review + dashboard + sidebar
================================================================================

[x] 1.1 Backend: extend /api/sales GET (apiPlugin.ts + [...path].ts)
       - Add start_date / end_date query filters (backwards-compatible)
       - Add optional payment_method filter
[x] 1.2 New src/pages/app/Sales.tsx (view-only):
       - Defaults: last 7 days, all stores, all payment methods
       - Filters: date range, store, payment method
       - Summary bar: count, total, discounts (formatCurrency)
       - Transaction table with expandable items (no void/edit)
[x] 1.3 AppRouter: add /app/:slug/sales route
[x] 1.4 Sidebar: new "Sales" section (icon , "Sales Review")
       directly below Dashboard
[x] 1.5 Sidebar: new "Reporting" section (icon ) directly ABOVE Sales
[x] 1.6 Dashboard (src/pages/app/Dashboard.tsx):
       - Two stat cards: "Sales" (count) + "Total Sold (currency)"
       - Date-range filter above grid (Today / 7 days / 30 days / custom),
         default 7 days
[x] 1.7 Verify: tsc + build pass. Commit NOT done (awaiting user approval)

================================================================================
PHASE 2 — Share-link styling + Reporting placeholder
================================================================================

[x] 2.1 index.css: new accent-outline button style (contrast against
       page background)
[x] 2.2 LoyaltyCards.tsx: apply accent-outline to "Share Registration Link"
       button; replace alert() with addToast; show link in copyable chip
[x] 2.3 New src/pages/app/Reporting.tsx "coming soon" placeholder page
[x] 2.4 Wire Reporting placeholder page to sidebar item (route + nav)
[x] 2.5 Verify: tsc + build pass. Commit NOT done (awaiting user approval)

================================================================================
PHASE 3 — Terminal scale weight popup
================================================================================

[x] 3.1 New WeightModal component:
       - PLU name + price/kg display
       - Live scale weight when connected (connect + poll)
       - Manual kg entry (3 decimal places)
       - Confirm / Cancel
[x] 3.2 Till.tsx: every scale-PLU click / barcode scan opens the popup
       (always prompt); remove broken auto-weight path
[x] 3.3 appStore.ts: fix addToBasket to respect passed quantity
       (scale items accumulate kg on re-add, do not force quantity 1)
[x] 3.4 Verify: tsc + build pass. Commit NOT done (awaiting user approval)

================================================================================
PHASE 4 — Receipt print overhaul
================================================================================

[x] 4.1 Till.tsx handlePrint: dedicated 80mm thermal print stylesheet
       (monospace, store header, date + txn number, aligned item columns,
       discount line, bold TOTAL, payment line, footer)
[x] 4.2 ReceiptModal: match on-screen layout to print layout
[x] 4.3 Verify: tsc + build pass. Commit NOT done (awaiting user approval)

================================================================================
PHASE 5 — Final verification
================================================================================

[x] 5.1 npx tsc --noEmit (app + server + api strict checks)
[x] 5.2 npm run build
[x] 5.3 Full dev smoke test — DONE, superseded by later E2E runs. Full
       DEV+PROD flows validated in smoke_test_results/20260811_smoke_report.md
       (commit 364ff1f): POS login -> scale/weighted sale, receipt, HO Sales
       Review + Dashboard, share link, Reporting all exercised. Later commits
       (dc3b0c6, 24577d4) continue this.
[x] 5.4 Confirmed 2026-08-16 against live project: 006_security_hardening.sql
       applied (sales_transactions exposes cashback_earned/cashback_percent).

================================================================================
PHASE 6 — Full system check (post-deploy verification)
================================================================================

[x] 6.1 Deployment health
      - vercel inspect: latest production deployment = Ready, contains
        all 3 functions (api/[...path], api/users/[id], api/users/create)
      - Confirm current production alias new-sales-system-rho.vercel.app
        points at the latest deployment
      - Note: new-sales-system.vercel.app is a STALE alias (SPA 200 but
        /api 404) - do not use it for testing

[x] 6.2 Runtime error scan
      - vercel logs <deploy>: NO ERR_MODULE_NOT_FOUND, NO 500s after smoke

[x] 6.3 Public-route probe matrix (curl, expect specific codes):
      - GET  /api/stores?tenant_schema=<real tenant>  -> 200 JSON
      - POST /api/pos/pin-login (empty body)          -> 400 (route alive)
      - GET  /api/app/tenant-info                     -> 200/400 JSON
      - GET  /api/public/tenants                      -> 200 JSON
      - GET  /                                        -> 200 HTML
      - GET  /app/<slug> deep link                    -> 200 HTML (SPA rewrite)
      - GET  /pos/select-store                        -> 200 HTML (SPA rewrite)

[x] 6.4 Protected-route probe matrix (curl, no session -> expect 401 JSON,
      proving the function executes; each must be 401, NOT 500):
      - GET /api/logbook, /api/suppliers, /api/plu, /api/sales,
        /api/plu_scheduled_changes/due,
        /api/settings/loyalty-cashback-percent, /api/users/create (GET),
        /api/admin/tenants, /api/admin/stores

[x] 6.5 Deep authenticated checks — DONE. Deep auth flows validated in
       smoke_test_results/20260811_smoke_report.md (POS PIN login, staff
       allocation, goods-in receive with PIN, sale with PIN; DEV + PROD).
[x] 6.6 Interactive user checklist — DONE via the 2026-08-11 E2E smoke
       (HO login, logbook populated, suppliers/PLU, POS PIN login + sale,
       loyalty cards) on both DEV and PROD.
[x] 6.7 Local dev regression: npm run dev -> stores 200, logbook page loads
       (vite plugin path unaffected by .js change)

[x] 6.8 Mark 5.3/5.4 [x] once 6.6 passes; record results in plan — DONE
       2026-08-16 (see smoke report). NOTE: 20260811Plan.txt is historical;
       outstanding discrepancies (D6/D7/D8, weighted-item stock units) are
       tracked in implementation_plans/20260816Plan.txt.


================================================================================
NOTES / DECISIONS (locked in 2026-08-11)
================================================================================
- Dashboard sales cards: date-range filter (Today/7d/30d/custom), default 7d
- No extra dashboard cards beyond Sales count + Total Sold
- Reporting = clickable "coming soon" page
- Weight popup: ALWAYS prompts on scale-PLU select (live weight when connected)
- Sales Review defaults: last 7 days, all stores, all payment methods
- All money displays use formatCurrency util (currencyConfig symbol)
- Both server implementations (Express apiPlugin.ts + Vercel [...path].ts)
  must stay in sync
- 0c/0d dev smoke (2026-08-11, port 5174, temp SERVICE_ROLE-minted session):
  create with unique PIN 200; duplicate PIN 400 "This PIN is already in use
  by another user in this tenant"; self-PIN change 200 (self excluded);
  cross-user PIN change 400; unique PIN change 200. Test users cleaned up.
- 0c/0d prod check (2026-08-11, commit 77f904c, deployment n6pn1h444):
  public routes 200, protected routes 401, pin-login 400, log scan clean
  (no ERR_MODULE_NOT_FOUND / FUNCTION_INVOCATION_FAILED / 500s)
- 0e/0f/0g dev smoke (2026-08-11, port 5174, temp users minted + cleaned):
  All green (1 environment-caused 429 on repeated wrong-PIN check — throttle
  verified working; 401 wrong-PIN verified in preceding clean run):
  - tenant admin pin-login -> kind:admin + tenant_schema + pending token
  - admin/stores with pending token (own schema) 200 (3 stores);
    other schema 403; admin/tenants 403 (tenant admin restricted)
  - admin-finalize wrong tenant 403; correct store -> pos token role admin
  - clock-in/clock-status/clock-out with staff pin on ADMIN terminal ->
    allocated to staff user; wrong pin 401; good PIN 429 after repeats
  - staff pin-login regression -> kind:user + assigned store
  - sale with staff pin on admin terminal -> staff allocated;
    wrong pin 401; no-pin own-token legacy path works
  - goods-in receive with staff pin -> success; logbook entry written
    ("received_by, Smoke Staff"); wrong pin 401
  Runtime alert for the user: received_by column lands only after running
  supabase/migrations/007_add_received_by.sql (DO-block over public.tenants,
  run once); route
  retries without the column pre-migration so nothing breaks meanwhile.
